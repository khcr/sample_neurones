<% @title = 'Galleries | new' %>

<h1>Add a gallery</h1>

<%= form_for @gallery do |f| %>

	<%= render 'shared/error_messages', object: f.object %>

	<%= render 'fields', f: f %>

	<%= f.submit "Create" %>
	
<% end %>