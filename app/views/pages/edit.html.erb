<% @title = 'Pages | Edit' %>

<h1>Lorem ipsum</h1>

<h2>Notre vision</h2>

<%= form_for @page do |f| %>
	
	<%= render 'shared/error_messages', object: f.object %>

	<%= f.label :content %>
	<%= f.text_area(:content, size: '50x5') %>

	<%= f.submit "Edit" %>

<% end %>